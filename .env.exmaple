STOCK_CODES=098120
HEADLESS_MODE=true

# 목표주가 대비 현재가의 차이 비율 (예: 20%)
PRICE_GAP_PERCENTAGE=20

# Slack 봇 토큰 (xoxb- 로 시작)
SLACK_TOKEN=XXXXX

# 메시지를 보낼 Slack 채널 ID
SLACK_CHANNEL_ID=XXX

# --- 실행 주기 설정 (Cron 표현식) ---
# 분 시 일 월 요일 (0 * * * 1-5 -> 월~금요일 매시 정각)
CRON_SCHEDULE='0 8 * * 1-5'
# CRON_SCHEDULE='0 7 * * *'

# 최소 시가총액 기준 (단위: 억)
# 예: 5000 -> 5,000억 원 이상인 종목만 알림 대상
MIN_MARKET_CAP_BILLIONS=5000
